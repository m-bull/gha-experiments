---
name: Test exit codes
on:
  workflow_dispatch:

jobs:
  test-job:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Test exit codes
        shell: bash
        run: echo -e "v0.12.0\n${VERSION}" | sort -V -C
        env:
          VERSION: "v0.12.3"


